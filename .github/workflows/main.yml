name: Cypress Tests

on: push

jobs:
  cypress-run:
    working_directory: ~AshrafShaik001/Sample-timeline/e2e-tests
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Cypress run
        uses: cypress-io/github-action@v6
        with:
          browser: chrome
          # group: "All tests"
          headed: false
          record: false
          parallel: false
          config: baseUrl="http://planning-staging.timeline.co/"
